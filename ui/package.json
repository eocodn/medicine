{
  "name": "medicine-shared-ui",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json --outDir dist && cp -R public/. dist/",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
