services:
  dur:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: ["python", "-m", "medicine_dur.cli"]

  test:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: ["python", "-m", "unittest", "discover", "-s", "tests", "-v"]
