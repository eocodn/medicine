__pycache__/
*.pyc
.pytest_cache/
.env
gha-creds-*.json

# DUR source and generated data are intentionally kept out of Git.
data/raw/
data/kids/
data/db/
data/reference/
data/backup/
data/canonical/

# Android Gradle outputs and local caches.
android/.gradle/
android/build/
android/app/build/
android/**/*.jks
android/**/*.keystore

# OCR local private inputs must stay out of Git.
browser_ocr/finetune/private/

# Detector model caches are reproducible from pinned manifests.
browser_ocr/detection/.cache/
browser_ocr/mobile/work/
android/.kotlin/
.android-gradle-cache/

# Rust build outputs are worktree-local and reproducible.
rust/**/target/

# Shared UI build dependencies are reproducible from ui/package-lock.json.
ui/node_modules/
