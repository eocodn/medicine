{
  "name": "medicine-browser-ocr-assets",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test test/*.test.js",
    "detection:test": "node --test detection/tests/*.test.mjs",
    "detection:validate": "node detection/cli.mjs validate --corpus detection/corpus/manifest.json --json",
    "probe-browser": "node probe_browser.mjs",
    "model:eval": "node eval/run_eval.mjs"
  },
  "dependencies": {
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "esbuild": "0.25.8",
    "js-yaml": "4.3.1"
  }
}
